variables:
- name: configuration
  value: Release
- name: shell
  value: bash
- name: command
  value: ls -l

- group: global

  

pool:
 name: UbuntuVM
 
# steps:
# - checkout: none
# - ${{ variables.shell }}: ${{ variables.command }} 
# - bash:  "echo connecting to :$(password)" 

steps:
  - bash: |
      "echo we are deploying on ${{ dotnet_version }} and the 
        running the app is {{ configuration }} mode"
        