variables:
- name: configuration
  value: Release
- name: shell
  value: bash
- name: command
  value: ls -l

  

pool:
 name: UbuntuVM
 
steps:
- checkout: none
- ${{ variables.shell }}: ${{ variables.command }} 
- script:  "echo connecting to :$(password)" 
